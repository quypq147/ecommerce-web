@model EcommerceApp.Models.Product

@{
    ViewData["Title"] = Model.Name + " - Đánh giá";
}

<style>
    body {
        background-color: #f5f5f5;
    }

    .reviews-container {
        background-color: white;
        border-radius: 8px;
        padding: 30px;
        margin: 20px 0;
        box-shadow: 0 2px 8px rgba(0,0,0,0.1);
    }

    .product-header {
        background-color: white;
        padding: 20px;
        border-radius: 8px;
        margin-bottom: 30px;
        border-left: 5px solid #DC143C;
    }

    .product-header h2 {
        color: #333;
        margin-bottom: 10px;
    }

    .product-header p {
        color: #666;
        margin: 0;
    }

    .section-title {
        color: #333;
        font-size: 24px;
        font-weight: 700;
        margin-bottom: 20px;
        padding-bottom: 10px;
        border-bottom: 3px solid #DC143C;
    }

    .stars {
        display: inline-flex;
        gap: 2px;
    }

    .star {
        color: #FFD700;
        font-size: 16px;
    }

    .star.empty {
        color: #ddd;
    }

    .review-form {
        background-color: #f9f9f9;
        padding: 20px;
        border-radius: 8px;
        border-left: 5px solid #DC143C;
        margin-bottom: 30px;
    }

    .review-form h4 {
        color: #333;
        margin-top: 0;
    }

    .form-label {
        color: #333;
        font-weight: 600;
        margin-bottom: 8px;
    }

    .form-control,
    .form-select {
        border: 1px solid #ddd;
        border-radius: 6px;
        padding: 10px;
        font-size: 14px;
    }

    .form-control:focus,
    .form-select:focus {
        border-color: #DC143C;
        box-shadow: 0 0 0 3px rgba(220, 20, 60, 0.1);
    }

    .btn-submit {
        background-color: #DC143C;
        color: white;
        border: none;
        padding: 10px 25px;
        border-radius: 6px;
        font-weight: 600;
        cursor: pointer;
        transition: all 0.3s ease;
    }

    .btn-submit:hover {
        background-color: #b31130;
        color: white;
        text-decoration: none;
    }

    .btn-back {
        background-color: #e9ecef;
        color: #333;
        border: none;
        padding: 10px 25px;
        border-radius: 6px;
        font-weight: 600;
        cursor: pointer;
        transition: all 0.3s ease;
    }

    .btn-back:hover {
        background-color: #dee2e6;
        color: #333;
        text-decoration: none;
    }

    .review-item {
        background-color: #f9f9f9;
        padding: 20px;
        border-radius: 8px;
        margin-bottom: 15px;
        border-left: 4px solid #DC143C;
    }

    .review-header {
        display: flex;
        justify-content: space-between;
        align-items: center;
        margin-bottom: 10px;
    }

    .reviewer-name {
        color: #333;
        font-weight: 600;
        font-size: 16px;
    }

    .review-date {
        color: #999;
        font-size: 12px;
    }

    .review-rating {
        margin: 10px 0;
    }

    .review-comment {
        color: #333;
        line-height: 1.6;
        margin-top: 10px;
    }

    .no-reviews {
        text-align: center;
        padding: 40px 20px;
        color: #999;
    }

    .no-reviews p {
        font-size: 16px;
        margin: 0;
    }

    .back-link {
        display: inline-block;
        margin-bottom: 20px;
        color: #DC143C;
        text-decoration: none;
        font-weight: 600;
    }

    .back-link:hover {
        text-decoration: underline;
    }

    .btn-group {
        display: flex;
        gap: 10px;
        margin-top: 20px;
    }

    .stars-input {
        display: flex;
        gap: 10px;
    }

    .star-input {
        cursor: pointer;
        font-size: 24px;
        color: #ddd;
        transition: color 0.2s ease;
    }

    .star-input:hover,
    .star-input.active {
        color: #FFD700;
    }
</style>

<div class="container my-4">
    <a href="@Url.Action("Details", "Store", new { id = Model.Id })" class="back-link">← Quay lại sản phẩm</a>

    <div class="reviews-container">
        <div class="product-header">
            <h2>@Model.Name</h2>
            <p>Để lại đánh giá và xếp hạng cho sản phẩm này</p>
        </div>

        <h3 class="section-title">Thêm đánh giá của bạn</h3>

        <div class="review-form">
            <form method="post" action="@Url.Action("Create", "Review")">
                @Html.AntiForgeryToken()

                <input type="hidden" name="productId" value="@Model.Id">

                <div class="mb-3">
                    <label for="reviewerName" class="form-label">Tên của bạn</label>
                    <input type="text" class="form-control" id="reviewerName" name="reviewerName" placeholder="Nhập tên của bạn" required>
                </div>

                <div class="mb-3">
                    <label for="rating" class="form-label">Đánh giá</label>
                    <select class="form-select" id="rating" name="rating" required>
                        <option value="">Chọn số sao</option>
                        <option value="1">⭐ 1 Sao - Kém</option>
                        <option value="2">⭐⭐ 2 Sao - Tạm</option>
                        <option value="3">⭐⭐⭐ 3 Sao - Tốt</option>
                        <option value="4">⭐⭐⭐⭐ 4 Sao - Rất tốt</option>
                        <option value="5">⭐⭐⭐⭐⭐ 5 Sao - Xuất sắc</option>
                    </select>
                </div>

                <div class="mb-3">
                    <label for="comment" class="form-label">Nhận xét của bạn</label>
                    <textarea class="form-control" id="comment" name="comment" rows="5" placeholder="Chia sẻ cảm nhận của bạn về sản phẩm này..." required></textarea>
                </div>

                <div class="btn-group">
                    <button type="submit" class="btn-submit">Gửi đánh giá ✓</button>
                    <a href="@Url.Action("Details", "Store", new { id = Model.Id })" class="btn-back">Hủy</a>
                </div>
            </form>
        </div>

        <h3 class="section-title">Đánh giá khách hàng</h3>

        <div class="no-reviews">
            <p>Chưa có đánh giá nào. Hãy là người đầu tiên đánh giá sản phẩm này!</p>
        </div>
    </div>
</div>
